import { PageHero } from "@/components/page-hero"

export function Header() {
  return (
    <PageHero
      badge="Détection propulsée par 4 modèles d'IA"
      title="Cette image est-elle"
      highlight="réelle ou générée par IA ?"
    />
  )
}
